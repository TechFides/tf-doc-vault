import { createTheme } from "@techfides/ana-docs/theme";
import "./custom.css";

export default createTheme({
  // Přepínač šířky obsahu (default → wide → max) v navbaru. Default false.
  widthToggle: true,
});

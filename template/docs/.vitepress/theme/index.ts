import { createTheme } from "@techfides/tf-doc-vault/theme";
import "./custom.css";

export default createTheme({
  // Content width toggle (default → wide → max) in the navbar. Default false.
  widthToggle: true,
});

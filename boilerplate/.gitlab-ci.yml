image: node:24@sha256:19cd848a0e073d34bd8cd5545a1b6b4d28489b3e3b607366621ced442bd5f6b4

variables:
  PNPM_STORE: "$CI_PROJECT_DIR/.pnpm-store"
  # Filling these in switches the runtime to `nginx-auth`; leave blank otherwise.
  # Not a secret: access to the repo already means access to the app.
  BASIC_AUTH_USER: "__BASIC_AUTH_USER__"
  BASIC_AUTH_PASS: "__BASIC_AUTH_PASS__"

stages:
  - install
  - lint
  - build
  - deploy

# ── Install ───────────────────────────────────────────────────────────────────

📦 install:
  stage: install
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      interruptible: true
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  before_script:
    - corepack enable
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/".insteadOf "ssh://git@gitlab.com/"
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/".insteadOf "git@gitlab.com:"
  script:
    - pnpm config set store-dir $PNPM_STORE
    - pnpm install --frozen-lockfile
  tags:
    - docker-privileged
  cache:
    key: "$CI_COMMIT_REF_NAME-$CI_PIPELINE_ID"
    paths:
      - node_modules/
      - .pnpm-store/
  variables:
    KUBERNETES_POD_LABELS_1: section=install_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=install

# ── Lint ──────────────────────────────────────────────────────────────────────

🧹 lint:typecheck:
  stage: lint
  needs: ["📦 install"]
  before_script: [corepack enable]
  cache:
    key: "$CI_COMMIT_REF_NAME-$CI_PIPELINE_ID"
    paths: [node_modules/, .pnpm-store/]
    policy: pull
  tags: [docker-privileged]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      interruptible: true
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  script:
    - pnpm run typecheck
  variables:
    KUBERNETES_POD_LABELS_1: section=lint_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=typecheck

🧹 lint:eslint:
  stage: lint
  needs: ["📦 install"]
  before_script: [corepack enable]
  cache:
    key: "$CI_COMMIT_REF_NAME-$CI_PIPELINE_ID"
    paths: [node_modules/, .pnpm-store/]
    policy: pull
  tags: [docker-privileged]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      interruptible: true
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  script:
    - pnpm run lint
    - pnpm run format:check
  variables:
    KUBERNETES_POD_LABELS_1: section=lint_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=eslint

🧹 lint:docs:
  stage: lint
  needs: ["📦 install"]
  before_script: [corepack enable]
  cache:
    key: "$CI_COMMIT_REF_NAME-$CI_PIPELINE_ID"
    paths: [node_modules/, .pnpm-store/]
    policy: pull
  tags: [docker-privileged]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      interruptible: true
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  script:
    - pnpm run docs:validate
  variables:
    KUBERNETES_POD_LABELS_1: section=lint_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=docs

# ── Build ─────────────────────────────────────────────────────────────────────

🐳 build:docs:
  stage: build
  needs: ["🧹 lint:typecheck", "🧹 lint:eslint", "🧹 lint:docs"]
  image: google/cloud-sdk:slim@sha256:cb08e90df56365a0a0b68e019ca06f90174bc064c4b0a5edfc3a5cf4415b5ec0
  services:
    - name: docker:29.4-dind@sha256:685b91dca8eab7de1dce1c303dbb7a763e4082d6a60db10968adf3295fbd2495
      command: ["--registry-mirror", "https://mirror.gcr.io"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    KUBERNETES_POD_LABELS_1: section=build_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=build
  rules:
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  before_script:
    - apt-get update -qq && apt-get install -y -qq --no-install-recommends docker-cli netcat-openbsd
    - echo "$GCP_SA_KEY" | gcloud auth activate-service-account --key-file=-
    - gcloud auth configure-docker ${GCP_REGION}-docker.pkg.dev --quiet
    - export IMAGE="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT}/${SERVICE_NAME:-docs-web}/${SERVICE_NAME:-docs-web}"
    - export DATE_TAG=$(date -u -d "$(echo "$CI_COMMIT_TIMESTAMP" | cut -d+ -f1 | sed 's/T/ /')" +"%Y%m%d-%H%M")
    - if [ -n "$BASIC_AUTH_USER" ]; then SERVER_TYPE=nginx-auth; else SERVER_TYPE=__SERVER_TYPE__; fi
    - export BUILD_ARGS="--build-arg SERVER_TYPE=$SERVER_TYPE --build-arg GITLAB_CI_TOKEN=$CI_JOB_TOKEN"
    - if [ -n "$BASIC_AUTH_USER" ]; then BUILD_ARGS="$BUILD_ARGS --build-arg BASIC_AUTH_USER=$BASIC_AUTH_USER"; fi
    - if [ -n "$BASIC_AUTH_PASS" ]; then BUILD_ARGS="$BUILD_ARGS --build-arg BASIC_AUTH_PASS=$BASIC_AUTH_PASS"; fi
  tags:
    - docker-privileged
  script:
    - while ! nc -z docker 2375; do sleep 0.1 ; done
    - docker build $BUILD_ARGS --no-cache --tag "${IMAGE}:${DATE_TAG}" --tag "${IMAGE}:latest" .
    - docker push "${IMAGE}:${DATE_TAG}"
    - docker push "${IMAGE}:latest"

# ── Deploy ────────────────────────────────────────────────────────────────────

🚀 deploy:docs:
  stage: deploy
  needs: ["🐳 build:docs"]
  image: google/cloud-sdk:slim@sha256:cb08e90df56365a0a0b68e019ca06f90174bc064c4b0a5edfc3a5cf4415b5ec0
  environment:
    name: production
  dependencies: []
  tags: [docker-privileged]
  variables:
    KUBERNETES_POD_LABELS_1: section=deploy_resources
    KUBERNETES_POD_LABELS_2: project=__PROJECT_DASHED__
    KUBERNETES_POD_LABELS_3: step=docs
  rules:
    - if: '$CI_PIPELINE_SOURCE != "merge_request_event" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      interruptible: true
    - when: never
  before_script:
    - echo "$GCP_SA_KEY" | gcloud auth activate-service-account --key-file=-
    - export IMAGE="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT}/${SERVICE_NAME:-docs-web}/${SERVICE_NAME:-docs-web}"
    - export DATE_TAG=$(date -u -d "$(echo "$CI_COMMIT_TIMESTAMP" | cut -d+ -f1 | sed 's/T/ /')" +"%Y%m%d-%H%M")
  script:
    - >
      gcloud run deploy ${SERVICE_NAME:-docs-web}
      --image="${IMAGE}:${DATE_TAG}"
      --region=${GCP_REGION}
      --project=${GCP_PROJECT}
      --quiet
    - >
      gcloud run services describe ${SERVICE_NAME:-docs-web}
      --region=${GCP_REGION}
      --project=${GCP_PROJECT}
      --format='value(status.url)'
